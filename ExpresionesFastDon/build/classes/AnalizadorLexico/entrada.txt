// Clase  main: punto de entrada del programa
    main() {
        // Declaración de una variable booleana
        boolean asiste;         // Variable booleana sin valor
        boolean     acceso = true;  // Variable para el control de acceso
        


        // Declaración de variables enteras
        int      suma = 0   ;        // Acumulador de la suma
        int  suma= 0   ;        // Acumulador de la suma
        int cantidad  ;             // Variable de control del bucle
        int edad = 21;
        int edad = 30;
        int;
        int jaime;
        int mindy;
        String nombre;      // Variable nombre de tipo String (cadena de caracteres)
        String escuela = "tsj"; // Variable escuela de tipo String (cadena de caracteres)
        boolean esta = true;
        String escuela = "Hola";


        // Bucle for: recorre valores de 0 a 9
        for (int i = 0; i < 10; i++) {
            
            // Condicional if: 
            if (jaime==29 && mindy ==30) {
                
                // Operación aritmética: suma acumulada
                suma = suma + i;

            }
        }
        


        int opcion;
        inputI(opcion);
        
        // Condicional switch:
        switch(opcion){
        case 1:
        print("Hola");
        break;
        default:
        break;
        }

        inputS(nombre);
        inputB(esverdadero);
        inputI(edad);
        
        while(opc==30){
        edad = 30;

        }

        for(j; j<10; j++){

        Vanessa = 20;

}

        // Salida por consola: muestra el resultado final
        print("La suma de los pares es: " + suma);
    }
